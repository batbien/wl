#!/bin/bash
SERVER=ssh.strato.de
dir=$1
cd $dir
# sftp $FTP_USER@$SERVER <<EOF
# yes
# $FTP_PWD
# mkdir $dir
# cd $dir
# mput *
# close
# bye

sshpass -p $FTP_PWD -o BatchMode=no -b - $FTP_USER@$server << !
   mkdir $dir
    cd $dir
    mput *
    close
    bye
!
